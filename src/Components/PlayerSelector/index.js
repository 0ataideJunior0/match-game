function PlayerSelector(Player1, Player2) {
  return;

  parameter.Player1, parameter.Player2;

  /*
`
    <article class = "player-selector">
    Player1, Player2
    <article/>
    
    `; */
}

export default PlayerSelector;
